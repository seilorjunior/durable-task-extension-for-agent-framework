# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: durable-agents-travel-planner
metadata:
  template: durable-agents-travel-planner@0.0.1-beta

infra:
  provider: bicep
  path: ./infra

services:
  api:
    project: ./api
    language: python
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .

  web:
    project: ./frontend
    language: js
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
